// This file previously contained JSX but used a .ts extension which caused parser errors.
// The real TSX component lives in `AuthContentType.tsx` — keep this file as a harmless re-export
// so TypeScript doesn't try to parse JSX from a .ts file.

export {};